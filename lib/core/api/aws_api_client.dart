class AwsApiClient {
  const AwsApiClient();

  Future<String?> read(String key) {
    throw UnimplementedError('AWS API client is not configured yet.');
  }

  Future<void> write(String key, String value) {
    throw UnimplementedError('AWS API client is not configured yet.');
  }
}
